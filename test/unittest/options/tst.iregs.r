7
iregs 8 gives 0
iregs 7 gives 1
iregs 6 gives 1
iregs 5 gives 1
iregs 4 gives 1
iregs 3 gives 1
-- @@stderr --
dtrace: failed to set -x iregs: Insufficient registers to generate code
dtrace: failed to set -x iregs: Insufficient registers to generate code
dtrace: failed to set -x iregs: Insufficient registers to generate code
dtrace: failed to set -x iregs: Insufficient registers to generate code
dtrace: failed to set -x iregs: Insufficient registers to generate code
