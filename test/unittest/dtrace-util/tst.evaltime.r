Constructor run.
main() run.
dynamic loader syscalls seen
evaltime is exec, trigger is visible-constructor, 2 write()s.

Constructor run.
main() run.
evaltime is preinit, trigger is visible-constructor, 2 write()s.

Constructor run.
main() run.
evaltime is postinit, trigger is visible-constructor, 2 write()s.

Constructor run.
main() run.
evaltime is main, trigger is visible-constructor, 2 write()s.

Constructor run.
main() run.
dynamic loader syscalls seen
evaltime is exec, trigger is visible-constructor-unstripped, 2 write()s.

Constructor run.
main() run.
evaltime is preinit, trigger is visible-constructor-unstripped, 2 write()s.

Constructor run.
main() run.
evaltime is postinit, trigger is visible-constructor-unstripped, 1 write()s.

Constructor run.
main() run.
evaltime is main, trigger is visible-constructor-unstripped, 1 write()s.

Constructor run.
main() run.
evaltime is default, trigger is visible-constructor, 2 write()s.

Constructor run.
main() run.
evaltime is default, trigger is visible-constructor-unstripped, 2 write()s.

