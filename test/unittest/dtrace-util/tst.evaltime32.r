Constructor run.
main() run.
evaltime is exec, trigger is visible-constructor-32, 2 write()s; dynamic loader syscalls seen.

Constructor run.
main() run.
evaltime is preinit, trigger is visible-constructor-32, 2 write()s; dynamic loader syscalls not seen.

Constructor run.
main() run.
evaltime is postinit, trigger is visible-constructor-32, 1 write()s; dynamic loader syscalls not seen.

Constructor run.
main() run.
evaltime is main, trigger is visible-constructor-32, 1 write()s; dynamic loader syscalls not seen.

Constructor run.
main() run.
evaltime is default, trigger is visible-constructor-32, 2 write()s; dynamic loader syscalls not seen.

