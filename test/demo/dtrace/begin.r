                   FUNCTION:NAME
                      mmap:entry mmap with prot = ---
                      mmap:entry mmap with prot = r--
                      mmap:entry mmap with prot = -w-
                      mmap:entry mmap with prot = rw-
                      mmap:entry mmap with prot = --x
                      mmap:entry mmap with prot = r-x
                      mmap:entry mmap with prot = -wx
                      mmap:entry mmap with prot = rwx
                      mmap:entry mmap with prot = rw-
                exit_group:entry 

-- @@stderr --
dtrace: script 'test/demo/dtrace/begin.d' matched 4 probes
