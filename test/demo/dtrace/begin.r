                   FUNCTION:NAME
                      mmap:entry mmap with prot = rw-
                      mmap:entry mmap with prot = r--
                      mmap:entry mmap with prot = r-x
                      mmap:entry mmap with prot = rw-
                exit_group:entry 

-- @@stderr --
dtrace: script 'test/demo/dtrace/begin.d' matched 3 probes
